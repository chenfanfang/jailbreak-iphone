#!/bin/bash
currentDir=`dirname $0`
filePath="$currentDir/$1"

    echo "=============================路径为================================"
    echo $filePath
    echo "=================================================================="